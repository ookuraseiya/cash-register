import React from 'react';

// 支出合計の計算
export const ExpenseTotal = (expenseItem) => {
  // const incomeArr = expenseItem.map((expenseItems) =>
  //   Number(expenseItems.number)
  // );
  // return incomeArr.reduce((a, c) => (a += c), 0);
};
